# Server Configuration
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# Database
DATABASE_FILE=./db/database.sqlite

# JWT Secret (generate a random string, e.g., use: openssl rand -base64 32)
JWT_SECRET=your-secret-key-here-change-this-in-production

# Session Cookie
SESSION_COOKIE_NAME=sid

# SMTP Configuration (Brevo)
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=your-brevo-smtp-username
SMTP_PASS=your-brevo-smtp-password
MAIL_FROM=AI Blog Generator <no-reply@yourdomain.com>

# OpenAI Configuration
OPENAI_API_KEY=sk-your-openai-api-key-here
OPENAI_MODEL=gpt-4o-mini

# AI Provider (OPENAI or GEMINI - only OPENAI implemented in MVP)
AI_PROVIDER=OPENAI

# Optional: Admin notification email
NOTIFY_TO=admin@yourdomain.com